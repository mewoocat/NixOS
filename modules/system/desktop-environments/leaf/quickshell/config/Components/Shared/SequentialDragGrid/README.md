This component is used to create a sequential block of items that can be dragged and dropped in different orders.

The limitation of this component is that all items will be placed next to each other.  This means that items cannot be placed at arbitrary x / y positions.
